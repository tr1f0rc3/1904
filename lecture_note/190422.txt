임시 객체의 주소를 받는건 불가능함.

forward 검색

ref qualifer &나 &&, const&로 구분. 하나 만들면 나머지도 다 만들어야 함.
모자르게 만들면 알아서 변환해줌. 에러 생김.
decltype 은 변수 의 타입을 추론함.
템플릿 리턴 시 decltype(auto)하면 다 추론함.

c++ 템플릿 1060p 번역 좋음.
uml distilled , uml 실전에서는 이것만 쓴다 추천.

생성자를 이용해 자동적으로 출력하거나 버그잡는것도 가능.

소멸자는 상속받아도 만들기;

퍼블릭함수는 꼭 예외처리하기

